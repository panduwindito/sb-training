# sb-training

Prerequisites:
- JDK 17
- Postgresql (create db manual with name = traindb)
- IDE (recommendation: IntelliJ)

Separate section for every section and every branch has its own note in notes folder.

1. Main:
   - Folder structure
   - Basic API
2. Validation:
   - add request validation
   - add custom exception
   - centralize exception handling
   - add format response